Конечно. Вот чистый и понятный README.md, который соответствует твоей архитектуре и инструментарию (Makefile, Docker, Goose embed).

Сохрани этот код в файл README.md в корне проекта.

code
Markdown
download
content_copy
expand_less
# Question Bank Service

REST API сервис для управления банком вопросов и генерации тестовых сессий.
Написан на Go с использованием Clean Architecture.

## 🛠 Технологии

- **Language:** Go
- **Database:** PostgreSQL 15
- **Framework:** Gin Web Framework
- **Migration:** Goose (встроены в бинарник)
- **Deployment:** Docker & Docker Compose

## 🚀 Быстрый запуск

Для запуска требуется только **Docker** и **Make** (есть в Linux/Mac, на Windows можно использовать WSL или запускать команды из Makefile вручную).

### 1. Запуск

Нужно создать файл .env и положить туда пароли из примера example.env
```bash
cp example.env .env
```

Приложение, база данных и миграции запустятся одной командой:

```bash
make docker-up
```

После запуска API будет доступно по адресу: http://localhost:8080

2. Остановка

Чтобы остановить контейнеры и удалить их:


```bash
make docker-down
```

🔌 Примеры запросов (API Examples)
1. Создать вопрос (Create Question)

POST /api/v1/questions

```bash
curl --location --request POST 'http://localhost:8080/api/v1/questions/' --header 'User-Role: admin' --data-raw '{
  "role": "backend_junior",
  "topic": "HTTP",
  "type": "single_choice",
  "difficulty": 3,
  "text": "What is HTTP?",
  "options": [
    {"text": "Protocol", "is_answer": true},
    {"text": "Language", "is_answer": false}
  ]
}'

```

🔧 Локальная разработка (без Docker)

Если вы хотите запустить приложение локально (через go run), вам всё равно понадобится база данных.

Поднять только базу:

```bash
docker-compose up -d db
```

Запустить приложение:

```bash
make run
```

Тесты:

```bash
make test
```

но тесты могут запускаться впервый раз долго, так как там поднимается БД

